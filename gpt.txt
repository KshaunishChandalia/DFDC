for group in image_groups:
    if len(group) > 1:
        images = []
        try:
            for path in group:
                with Image.open(path) as img:
                    images.append(img.convert("RGB").copy())  # Copy to release file
            
            buffer = BytesIO()
            images[0].save(buffer, format="PDF", save_all=True, append_images=images[1:])
            buffer.seek(0)
            
            b64_pdf = base64.b64encode(buffer.read()).decode("utf-8")
            pdf_base64_list.append(b64_pdf)
        
        except Exception as e:
            print(f"Error processing group {group}: {e}")
            pdf_base64_list.append(None)
        
        finally:
            del images  # Free memory
    else:
        pdf_base64_list.append(None)
-----------
def image_to_base64(path):
    with open(path, "rb") as image_file:
        return base64.b64encode(image_file.read()).decode("utf-8")

# Final result list
final_data = []

# Process and group by filename
for filename, img_path in image_data:
    img_b64 = image_to_base64(img_path)
    
    # Search for existing filename
    for i, (fname, b64_list) in enumerate(final_data):
        if fname == filename:
            b64_list.append(img_b64)
            break
    else:
        # Not found, so create new entry
        final_data.append((filename, [img_b64]))

# Optional: Generate new image names (not returned, but can be printed or used)
for filename, b64_list in final_data:
    for i, b64_img in enumerate(b64_list):
        new_name = f"output_image_{filename}_{i+1}"
        print(new_name)

----------
final_data = []
for filename, results in result_dict.items():
    if len(results) == 1:
        final_data.append((filename, results[0]))
    else:
        combined_result = ', '.join(f"{i+1}st page: {res}" if i == 0 else f"{i+1}nd page: {res}" if i == 1 else f"{i+1}th page: {res}" for i, res in enumerate(results))
        final_data.append((filename, combined_result))

# Output
for item in final_data:
    print(item)
-------
import pandas as pd
import numpy as np
import os
from deepface import DeepFace
from sklearn.metrics import confusion_matrix, accuracy_score
from tqdm import tqdm
from collections import defaultdict

# Load CSV
df = pd.read_csv('your_file.csv')  # Replace with actual file path

# DeepFace models
models = ["VGG-Face", "Facenet", "Facenet512", "OpenFace", "DeepFace", "DeepID", "ArcFace", "Dlib", "SFace"]

# Initialize logs for each metric
cosine_scores = defaultdict(list)
euclidean_scores = defaultdict(list)
euclidean_l2_scores = defaultdict(list)
angular_scores = defaultdict(list)

# Ground truth and predictions
ground_truths = []
predictions = []

# Define default thresholds
thresholds = {
    "cosine": 0.4,
    "euclidean": 10,
    "euclidean_l2": 0.6,
    "angular": 0.4,
}

# Angular distance calculation
def angular_distance(u, v):
    cos_sim = np.dot(u, v) / (np.linalg.norm(u) * np.linalg.norm(v))
    cos_sim = np.clip(cos_sim, -1.0, 1.0)
    return np.arccos(cos_sim) / np.pi

# Main loop
for idx, row in tqdm(df.iterrows(), total=len(df)):
    try:
        img1_path = os.path.join(row['foldername'], row['image_1_name'])
        img2_path = os.path.join(row['foldername'], row['image_2_name'])
        true_label = int(row['Ground truth'])
        
        match_found = False  # Default prediction

        for model_name in models:
            # Get embeddings
            emb1 = np.array(DeepFace.represent(img_path=img1_path, model_name=model_name, enforce_detection=False)[0]["embedding"])
            emb2 = np.array(DeepFace.represent(img_path=img2_path, model_name=model_name, enforce_detection=False)[0]["embedding"])

            # Compute distances
            cos_dist = 1 - (np.dot(emb1, emb2) / (np.linalg.norm(emb1) * np.linalg.norm(emb2)))
            euc_dist = np.linalg.norm(emb1 - emb2)
            euc_l2_dist = euc_dist / np.linalg.norm(emb1 + emb2)
            ang_dist = angular_distance(emb1, emb2)

            # Save distances
            cosine_scores[model_name].append(cos_dist)
            euclidean_scores[model_name].append(euc_dist)
            euclidean_l2_scores[model_name].append(euc_l2_dist)
            angular_scores[model_name].append(ang_dist)

            # Predict match for any metric
            if (
                cos_dist <= thresholds["cosine"] or
                euc_dist <= thresholds["euclidean"] or
                euc_l2_dist <= thresholds["euclidean_l2"] or
                ang_dist <= thresholds["angular"]
            ):
                match_found = True
                break  # Early stop on match

        predictions.append(int(match_found))
        ground_truths.append(true_label)

    except Exception as e:
        print(f"Error at index {idx}: {e}")
        predictions.append(0)
        ground_truths.append(int(row['Ground truth']))

# Evaluate
tn, fp, fn, tp = confusion_matrix(ground_truths, predictions).ravel()
accuracy = accuracy_score(ground_truths, predictions)

print(f"\nTP: {tp}, TN: {tn}, FP: {fp}, FN: {fn}")
print(f"Accuracy: {accuracy:.4f}")

##------------------

import matplotlib.pyplot as plt

# Example: Plot cosine distance distribution for ArcFace
plt.hist(cosine_scores['ArcFace'], bins=50, alpha=0.7, color='skyblue')
plt.axvline(x=thresholds['cosine'], color='red', linestyle='--', label='Threshold')
plt.title("Cosine Distance - ArcFace")
plt.xlabel("Cosine Distance")
plt.ylabel("Frequency")
plt.legend()
plt.grid()
plt.show()

#----------
pip install \
  nvidia-cublas-cu12==12.1.3.1 \
  nvidia-cuda-cupti-cu12==12.1.105 \
  nvidia-cuda-nvrtc-cu12==12.1.105 \
  nvidia-cuda-runtime-cu12==12.1.105 \
  nvidia-cudnn-cu12==8.9.2.26 \
  nvidia-cufft-cu12==11.0.2.4 \
  nvidia-curand-cu12==10.3.4.107 \
  nvidia-cusolver-cu12==11.4.5.107 \
  nvidia-cusparse-cu12==12.1.0.106 \
  nvidia-nvtx-cu12==12.1.105

##-----------------------------------------------------------------------------------------------------------------------------


Below is a **minimal, drop‑in logging upgrade** for the script you pasted.
It keeps the same single‑file structure so you can copy‑paste and run immediately, yet gives you:

* one‑line call to `get_logger()` anywhere
* configurable console level via `LOG_LEVEL` env‑var
* daily‑rotating file log (`aadhaar_pipeline.log`) in a `logs/` folder
* tracebacks and timing around the heavy steps (OCR, PDF decode, save)

```python
# ───────────────────────── logger.py ─────────────────────────
"""
Central logger usable from any module in this script‑based project.
Put this block at the very top of your file or in a small helper
module you can import anywhere.
"""
import logging
from logging.handlers import TimedRotatingFileHandler
import os
from pathlib import Path

def _build_logger() -> logging.Logger:
    level = os.getenv("LOG_LEVEL", "INFO").upper()
    log = logging.getLogger("aadhaar_pipeline")
    if log.handlers:          # already configured (avoid duplicates)
        return log

    log.setLevel(level)
    fmt = logging.Formatter(
        "%(asctime)s | %(levelname)s | %(name)s | %(message)s"
    )

    # Console handler
    ch = logging.StreamHandler()
    ch.setFormatter(fmt)
    log.addHandler(ch)

    # File handler (rotates at midnight, keeps 7 days)
    log_dir = Path("logs")
    log_dir.mkdir(exist_ok=True)
    fh = TimedRotatingFileHandler(
        log_dir / "aadhaar_pipeline.log", when="midnight", backupCount=7
    )
    fh.setFormatter(fmt)
    log.addHandler(fh)

    log.debug("Logger initialised with level %s", level)
    return log

LOGGER = _build_logger()

def get_logger(name: str | None = None) -> logging.Logger:  # convenience
    return LOGGER if name is None else LOGGER.getChild(name)
# ───────────────────────── end logger.py ─────────────────────
```

---

## 1  Wire it into your functions & classes

Below I show **only the lines that change** in your original script; everything
else stays as‑is.

```python
# add at the very top, right after imports
from logger import get_logger
log = get_logger(__name__)

# ───── pdf_bytes_to_images ─────
def pdf_bytes_to_images(pdf_bytes):
    images = []
    try:
        log.debug("Decoding PDF (%d bytes)", len(pdf_bytes))
        pdf_file = fitz.open(stream=pdf_bytes, filetype="pdf")
        for page_num in range(len(pdf_file)):
            page = pdf_file.load_page(page_num)
            pix = page.get_pixmap()
            img = Image.frombytes("RGB", [pix.width, pix.height], pix.samples)
            images.append(img)
        log.info("Converted %d page(s) of PDF to images", len(images))
    except Exception as e:
        log.exception("Error converting PDF")
    finally:
        if 'pdf_file' in locals() and pdf_file:
            pdf_file.close()
    return images
```

```python
# ───── MaskingTask.__init__ ─────
class MaskingTask:
    def __init__(self):
        self.log = get_logger(self.__class__.__name__)
        self.log.info("Loading PaddleOCR and paddlex models…")
        self.ocr_engine = PaddleOCR(use_angle_cls=True, lang="en")
        self.pdx_model = pdx.load_model("output/infer_model")
```

```python
# inside addhar_check
self.log.debug("Running OCR – %d boxes detected", len(result[0]))
```

```python
# ───── read_pdf_from_folder ─────
def read_pdf_from_folder(folder_path):
    masker = MaskingTask()
    stamper = StampingTask()
    log.info("Scanning folder %s", folder_path)
    i = 0
    for filename in os.listdir(folder_path):
        if filename.endswith(".txt"):
            log.debug("Processing %s", filename)
            ...
            img2.save(f"samples/samplesAugmented/output_image_{i}.jpg")
            log.info("Saved output_image_%d.jpg", i)
            i += 1
```

*Use `self.log.debug()` or `.info()` inside any helper you feel is critical.*
For heavy loops (OCR, image saving), wrap with `time.perf_counter()` if you
need latency metrics:

```python
start = time.perf_counter()
# … do work …
self.log.info("Masking finished in %.2f s", time.perf_counter() - start)
```

---

## 2  Run & verify

```bash
export LOG_LEVEL=DEBUG          # optional, defaults to INFO
python your_script.py
tail -f logs/aadhaar_pipeline.log
```

You should see clear statements like:

```
2025‑07‑15 09:12:43,812 | INFO  | MaskingTask | Loading PaddleOCR and paddlex models…
2025‑07‑15 09:12:46,331 | INFO  | __main__    | Scanning folder samples/samplesAugmented
2025‑07‑15 09:12:46,411 | DEBUG | __main__    | Processing doc1.txt
2025‑07‑15 09:12:48,902 | INFO  | __main__    | Saved output_image_0.jpg
```

---

### Why this is all you need for now

* **No external deps** – pure stdlib logging.
* **Thread/Fork safe** – each child process inherits the handlers.
* **Granular control** – switch to `WARNING`/`ERROR` in prod without code edits.

When you later split the codebase into modules (as in the scaffold we built),
you can keep using `from logger import get_logger` and every part of the
pipeline will log through the same sink.

Let me know if you’d like help adding timing decorators, JSON log output, or
integrating with a monitoring stack like Prometheus/Loki.

##-----------------------------------------------------------------------------------------------------------------------------
